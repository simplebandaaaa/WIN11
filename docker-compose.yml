version: '3.8'

services:
  windows:
    image: dockur/windows:latest
    ports:
      - "8006:8006"
      - "3389:3389"
    devices:
      - /dev/kvm
    cap_add:
      - NET_ADMIN
    environment:
      VERSION: "11"
      RAM_SIZE: "4G"
      CPU_CORES: "2"
    restart: on-failure
